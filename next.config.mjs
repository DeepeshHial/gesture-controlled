/** @type {import('next').Config} */
const nextConfig = {
  reactStrictMode: false, // Useful to prevent double-render in dev with Three.js sometimes, but let's keep false or true as needed.
};
export default nextConfig;
